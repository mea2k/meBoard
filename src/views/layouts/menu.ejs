<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/">meBoard</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
        aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item active">
                <a class="btn nav-link" href="/advert">Объявления <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item active">
                <a class="btn nav-link" href="/search">Поиск</a>
            </li>
            <li class="nav-item active">
                <a class="btn nav-link position-relative" href="/chat">
                    Чат
                    <% if (locals.chatStat?.newMsgs) { %>
                    <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
                        <small><%= locals.chatStat?.newMsgs %></small>
                        <span class="visually-hidden">непрочитанные сообщения</span>
                    </span>
                    <% } %>
                </a>
            </li>
        </ul>
    </div>
    <div class="text-right">
        <% if (user) { %>
            <span> Привет, <a href="/user/me">
                    <%= user.name %>
                </a>!</span><br />
            <a href="/user/logout">Выйти</a>
            <% } else { %>
                <a href="/user/login">Войти</a>
                <% } %>
    </div>
</nav>